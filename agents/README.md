# Agents Directory

This directory is dedicated to custom agent definitions, configurations, prompt files, and task-specific scripts for the **CreativeVisibility** project.

## Purpose

As the project grows, we can store specialized agent prompts, subagent configurations, or automated task scripts here. This ensures that any autonomous assistants deployed in this workspace have a shared context and structured instructions.
